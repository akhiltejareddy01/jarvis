# Sara Job backend — Phase: Scout + JD Parser + Fit Scorer (per Sara_Job_Arch.docx build order).
# Install: py -3.11 -m pip install -r requirements.txt  (inside .venv)

# --- API ---
fastapi==0.115.*
uvicorn[standard]==0.32.*

# --- Database ---
sqlalchemy==2.0.*
psycopg[binary]==3.2.*
pgvector==0.3.*

# --- Agents ---
langgraph==0.2.*
langchain-core==0.3.*

# --- Models ---
openai==1.*                # premium tier + Groq (OpenAI-compatible endpoint)
sentence-transformers==3.* # local nomic/bge embeddings, 768 dims

# --- Job source APIs ---
httpx==0.27.*

# --- Resume Selector ---
pypdf==6.*

# --- Config ---
pydantic==2.9.*
pydantic-settings==2.6.*
python-dotenv==1.0.*
PyYAML==6.*

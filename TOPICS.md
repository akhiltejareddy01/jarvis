Repository topics (recommended)

Add these topics to the repository on GitHub (Settings → Topics) or via the GitHub CLI:

- agent
- fastapi
- react
- python
- ml
- automation
- testing

To add via gh CLI:

```bash
gh repo edit akhiltejareddy01/jarvis --add-topic agent --add-topic fastapi --add-topic react --add-topic python --add-topic ml --add-topic automation --add-topic testing
```
